# Read the documentation at https://github.com/thephpleague/flysystem-bundle/blob/master/docs/1-getting-started.md
parameters:
    filesAvatarDir: '%kernel.project_dir%/public/avatar'
    filesAvatarUri: '/avatar/'

flysystem:
    storages:
        avatar.storage:
            adapter: local
            options:
                directory: '%filesAvatarDir%'
        projectFiles.storage:
            adapter: local
            options:
                directory: '%kernel.project_dir%/var/storage/default/uploads'
        licenses.storage:
            adapter: local
            options:
                directory: '%kernel.project_dir%/var/storage/licenses'
