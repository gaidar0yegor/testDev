{% extends 'corp_app/base.html.twig' %}

{% block title %}{{ 'affectation_groupe'|trans }} | {{ parent() }}{% endblock %}

{% block body %}
    <h1>{{ 'affectation_groupe'|trans }} : {{ projet.acronyme }}</h1>

    <div class="d-flex justify-content-between align-items-center">
        <a class="d-block mb-4" href="{{ path('corp_app_fo_projet_planning', {'projetId': projet.id}) }}">
            <i class="fa fa-chevron-left" aria-hidden="true"></i>
            {{ 'back'|trans }}
        </a>

        {% include 'corp_app/projets/_projet_actions_shortcut.html.twig' with {projet: projet} %}
    </div>

    {{ form_start(form) }}

    {{ form_errors(form) }}

    <table id="table_affectation_equipe" class="table w-100">
        <thead>
            <tr>
                <th>Tâche</th>
                <th>Participants</th>
            </tr>
        </thead>
        <tbody>
            {{ form_row(form.projetPlanningTasks) }}
        </tbody>
    </table>

    <div class="d-flex justify-content-center align-column-center">
        {{ form_row(form.submit) }}
    </div>

    {{ form_end(form) }}

{% endblock %}

    {% block stylesheets %}
        {{ parent() }}
        {{ encore_entry_link_tags('projet-gantt') }}
    {% endblock %}

    {% block javascripts %}
        {{ parent() }}
        {{ encore_entry_script_tags('projet-gantt') }}
    {% endblock %}