{{ evenement.createdBy.user.fullname }} a annulé l'évènement {{ evenement.text }} {% if evenement.projet %}sur le projet {{ evenement.projet.acronyme }}{% endif %}.

Voir sur RDI-Manager :

{{ url('corp_app_fo_current_user_events') }}