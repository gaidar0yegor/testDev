L’évènement {{ evenement.text }} a été annulé par {{ evenement.createdBy.user.fullname }} {% if evenement.projet %}sur le projet {{ evenement.projet.acronyme }}{% endif %}.

Voir sur RDI-Manager :

{{ url('corp_app_fo_current_user_events') }}