{% extends 'corp_app/base.html.twig' %}

{% block body %}

    <h1>{{ 'dashboards'|trans }}</h1>

    <h2>{{ 'menu.dashboard.create_view'|trans }}</h2>

    <div class="row">
        <div class="col-md-6 offset-md-3 col-sm-12">
            {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success">{{ 'menu.dashboard.create_view'|trans }}</button>

            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
