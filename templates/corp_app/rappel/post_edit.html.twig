{% extends 'corp_app/base.html.twig' %}

{% block title %}{{ 'create_rappel'| trans }} | {{ parent() }}{% endblock %}

{% block body %}

    <h1>{{ 'create_rappel' | trans }}</h1>

    <a class="d-block mb-4" href="{{ path('corp_app_fo_rappel_list') }}">
        <i class="fa fa-chevron-left" aria-hidden="true"></i>
        {{ 'back'|trans }}
    </a>

    <div class="row">
        <div class="col-lg-10 offset-lg-1">
            {{ form_start(form) }}
            {{ form_errors(form) }}

            <div class="row">
                <div class="col-md-4 col-sm-12">
                    {{ form_row(form.titre) }}
                </div>
                <div class="col-md-4 col-sm-12">
                    {{ form_row(form.reminderAt) }}
                </div>
                <div class="col-md-4 col-sm-12">
                    {{ form_row(form.societe) }}
                </div>
            </div>

            {{ form_row(form.description) }}
            <div class="d-flex justify-content-center w-100">
                {{ form_row(form.submit) }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
