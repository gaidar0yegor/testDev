 {{ form_start(form) }}
        {{ form_errors(form) }}

        {% if form.suspendedAt is defined %}
        {{ form_row(form.suspendedAt) }}
        {% endif %}
        {{ form_row(form.titre) }}
        {{ form_errors(form.description)}}
        {{ form_row(form.description)}}
        <small><p class="text-muted">Texte: <span id="count-carac"></span></p></small>
        <span id="fait_marquant_max_desc" class="d-none" data-limit="{{ userContext.societeUser.societe.faitMarquantMaxDesc }}" data-limitBlocking="{{ userContext.societeUser.societe.faitMarquantMaxDescIsblocking }}"></span>
        {{ form_row(form.fichierProjets) }}
        {{ form_row(form.date) }}
        <div class="text-center mt-5">
            <button class="btn btn-success">
                {{ button_label|default('Sauvegarder') }}
            </button>
        </div>

{{ form_end(form) }}