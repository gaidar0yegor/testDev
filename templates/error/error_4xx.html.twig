{% extends 'corp_app/base.html.twig' %}

{% block title %}{{ error.message }} | Erreur {{ error.statusCode }} | {{ parent() }}{% endblock %}

{% block body %}

<h1>
    {{ error.message }}
    <small class="blockquote-footer">Erreur {{ error.statusCode }}</small>
</h1>

<div class="row">
    <div class="col-md-8 offset-md-2">
        <p>
            Oups, il semblerait que

            {% if 404 == error.statusCode %}
                cette page n'existe pas.
            {% elseif 403 == error.statusCode %}
                vous n'ayez pas accès à cette page.
            {% elseif 409 == error.statusCode %}
                cette action soit impossible dans l'état actuel.
            {% else %}
                vous ayez tenté une action impossible.
            {% endif %}
        </p>
        <p>
            Vous pouvez soit revenir à la page précédente, soit
            <a href="{{ app.request.schemeAndHttpHost }}">revenir sur la page d'accueil</a>.
        </p>
    </div>
</div>

{% endblock %}
