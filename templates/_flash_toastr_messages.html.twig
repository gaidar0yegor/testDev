<script class="_flash_toastr_messages">
    {% for type, messages in app.flashes %}
        {% for message in messages %}
            window.toastr[`{{ type }}`](`{{ message | striptags('<a>') | raw }}`);
        {% endfor %}
    {% endfor %}
</script>
