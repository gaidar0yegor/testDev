{% extends 'lab_app/register/layout.html.twig' %}

{% block register %}
    <h1 class="mb-1">{{ 'my_rdi_manager_account'|trans }}</h1>

    <form method="post">
        <p class="text-center">
            <input type="hidden" name="csrf_token" value="{{ csrf_token('register_join_labo') }}" />
            <button
                type="submit"
                class="btn btn-lg btn-success"
            >{{ 'join_user_book'|trans }} <strong>{{ userBook.title }}</strong></button>
        </p>
    </form>
{% endblock %}
