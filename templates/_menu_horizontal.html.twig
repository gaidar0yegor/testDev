<nav class="navbar navbar-expand-lg navbar-dark">
    <a class="navbar-brand" href="{{ path('app_home') }}">
        <img src="{{ asset('img/Logo-RDI-blanc.png') }}" alt="RDI-Manager logo">
    </a>

    {% if app.user %}
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item {{ activeOn('app_fo_dashboard') }}">
                    <a class="nav-link" href="{{ path('app_fo_dashboard') }}">Tableau de bord</a>
                </li>
                <li class="nav-item dropdown {{ activeOn('app_fo_projets', 'app_fo_projets_all', 'app_fo_projets_by_year', 'app_fo_projet_creation') }}">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Mes Projets
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a
                            class="dropdown-item"
                            href="{{ path('app_fo_projets') }}"
                        >
                            <i class="fa fa-list-ul" aria-hidden="true"></i>Liste des projets
                        </a>
                        <a
                            class="dropdown-item {{ is_granted('ROLE_FO_CDP') ? '' : 'disabled' }}"
                            href="{{ path('app_fo_projet_creation') }}"
                            {% if not is_granted('ROLE_FO_CDP') %}
                                title="Vous devez avoir le rôle de chef de projet pour créer un nouveau projet."
                            {% endif %}
                        >
                            Créer un projet
                        </a>
                    </div>
                </li>
                <li class="nav-item dropdown {{ activeOn('app_fo_temps', 'app_fo_absences') }}">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Suivi temps
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a
                            class="dropdown-item"
                            href="{{ path('app_fo_temps') }}"
                        >
                            <i class="fa fa-tasks" aria-hidden="true"></i>Temps passés
                        </a>
                        <a
                            class="dropdown-item"
                            href="{{ path('app_fo_absences') }}"
                        >
                            <i class="fa fa-calendar" aria-hidden="true"></i>Mes absences
                        </a>
                    </div>
                </li>
                <li class="nav-item dropdown {{ activeOn('app_fo_admin_societe_show', 'app_fo_admin_timesheet_generate', 'app_fo_admin_utilisateurs', 'app_fo_admin_user_invite', 'app_fo_admin_projets', 'app_fo_admin_notification') }}">
                    <a
                        class="nav-link dropdown-toggle {{ is_granted('ROLE_FO_ADMIN') ? '' : 'disabled' }}"
                        {% if not is_granted('ROLE_FO_ADMIN') %}
                            title="Menu réservé à l'administrateur de votre société."
                        {% endif %}
                        href="#"
                        id="navbarDropdown"
                        role="button"
                        data-toggle="dropdown"
                        aria-haspopup="true"
                        aria-expanded="false"
                    >
                        Administration
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a
                            class="dropdown-item"
                            href="{{ path('app_fo_admin_societe_show') }}"
                        >
                            <i class="fa fa-building-o" aria-hidden="true"></i>Ma société
                        </a>
                        <a
                            class="dropdown-item"
                            href="{{ path('app_fo_admin_projets') }}"
                        >
                            Tous les Projets
                        </a>
                        <div class="dropdown-divider"></div>
                        <a
                            class="dropdown-item"
                            href="{{ path('app_fo_admin_utilisateurs') }}"
                        >
                            <i class="fa fa-users" aria-hidden="true"></i>Utilisateurs
                        </a>
                        <a
                            class="dropdown-item"
                            href="{{ path('app_fo_admin_timesheet_generate') }}"
                        >
                            <i class="fa fa-download" aria-hidden="true"></i>Export feuilles de temps
                        </a>
                        <a
                            class="dropdown-item"
                            href="{{ path('app_fo_admin_user_invite') }}"
                        >
                            <i class="fa fa-user-plus" aria-hidden="true"></i>Inviter un nouvel utilisateur
                        </a>
                        <div class="dropdown-divider"></div>
                        <a
                            class="dropdown-item"
                            href="{{ path('app_fo_admin_notification') }}"
                        >
                            <i class="fa fa-bell-o" aria-hidden="true"></i>Paramètres des notifications
                        </a>
                    </div>
                </li>
            </ul>

            <div class="btn-group user-menu">
                <a href="#" class="btn btn-outline-light dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {% if app.user.societe %}
                        {{ app.user.societe.raisonSociale }}
                        |
                    {% endif %}
                    {{ app.user.prenom }}
                    <i class="fa fa-lg fa-user-circle-o" aria-hidden="true"></i>
                </a>
                <div class="dropdown-menu">
                    {% if is_granted('ROLE_BO_USER') %}
                        <a
                            class="dropdown-item"
                            href="{{ path('app_bo_home') }}"
                        >
                            <i class="fa fa-rocket" aria-hidden="true"></i>Back Office
                        </a>
                        <div class="dropdown-divider"></div>
                    {% endif %}
                    <a
                        class="dropdown-item"
                        href="{{ path('app_fo_mon_compte') }}"
                    >
                        <i class="fa fa-user" aria-hidden="true"></i>
                        Mon compte
                    </a>
                    <div class="dropdown-divider"></div>
                    <a
                        class="dropdown-item"
                        href="{{ path('app_logout') }}"
                    >
                        <i class="fa fa-sign-out" aria-hidden="true"></i>
                        Déconnexion
                    </a>
                </div>
            </div>
        </div>
    {% endif %}
</nav>
