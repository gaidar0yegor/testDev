{% if matomoConfig.enabled %}
    <script>
        initMatomo('{{ matomoConfig.host }}',{{ 'demo' in app.request.getSchemeAndHttpHost() ? matomoConfig.demoSiteId : matomoConfig.prodSiteId }});
    </script>
{% endif %}
