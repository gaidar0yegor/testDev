{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouvel utilisateur | {{ parent() }}{% endblock %}

{% block body %}

<h1>Inviter un nouvel utilisateur</h1>
<h2>Saisir les informations</h2>

<div class="col-lg-6 mx-auto">
    <p>
        Vous pouvez envoyer une invitation par email ou sms à un utilisateur
        pour qu'il rejoigne votre société.
    </p>

    {{ form_start(form) }}
        {{ form_row(form.invitationEmail) }}
        {{ form_row(form.invitationTelephone) }}
        {{ form_row(form.role) }}

      	<div class="text-center mt-5">
        	{{ form_row(form.ajouter, { 'label' : bouton }) }}
		    </div>

    {{ form_end(form) }}

</div>

{% endblock %}
